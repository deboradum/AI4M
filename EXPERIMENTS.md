# Experiment index

This is the shared registry for scientific runs. Create or update a row before
starting a run; generated outputs stay under ignored `results/` and `volumes/`.

| ID | Parent | Ablation / change | Hypothesis | Important configuration | Status | Final result | Notes |
|---|---|---|---|---|---|---|---|
| E023_unet_aorta_75ep | E010_unet2d | Plain 2D U-Net on the recovered four-organ, HU-windowed target | E010's U-Net should remain viable when the aorta is a separate class | U-Net base 32, 4 levels, CE, `in_slices=1`, seed 123, no augmentation, 75 epochs; `dataset=SEGTHOR_aorta_huwide` | complete | 3D Dice: esophagus 0.471, heart 0.903, trachea **0.833**, aorta **0.801**; foreground mean **0.752** | Commit `c74ffa1`; 79.2 min on H100; best 2D validation Dice epoch 29. Close to E022 fg mean 0.757, with higher trachea/aorta but lower esophagus/heart. Descriptive only: E023 is U-Net+CE without augmentation; E022 is ENet+CE+Dice with augmentation. Patient_15 esophagus remains the major failure (Dice 0.011; HD95 152.7 mm). |
| E000 | – | Original course baseline | Reference implementation | ENet, CE, single slice, 25 epochs, unseeded | historical | – | Original run predates the YAML configuration workflow. |
| E001 | E000 | Reproducible baseline | A fixed seed provides a stable parent for ablations | `in_slices=1`, ENet 8/2, CE, seed 123, 25 epochs, SegTHOR 15/5 split | complete | 3D Dice: esophagus 0.610, heart 0.808, trachea 0.377; foreground mean 0.598 | Commit `617e3f1`; 21.4 min training; aorta unavailable in `segthor_part1`. |
| E006_cosine50 | – | Longer training with cosine learning-rate decay | No hypothesis recorded; the intent was that a decaying learning rate adds a point or two for free (plan item 1.6). | 50 epochs, CosineAnnealingLR (eta_min 0), ENet 8/2, CE, seed 123, 256x256, `dataset=SEGTHOR`, num_workers 5 | complete | 2D validation Dice 0.8436 at epoch 46 of 50. **CRITICAL**: `metrics_3d` is null — the 3D metrics were never computed, so this run cannot be compared with the 3D Dice in the other rows. | A100 job `26507842`; commit `e935b9c`; 18.5 min training; 764 MB peak GPU. Ran a FORK of the code (`source_sha256` lists a `train_16.py` plus modified `main.py`/`dataset.py`/`losses.py`), so the cosine scheduler never landed in the shared `main.py` and no current config can reproduce it. Its record calls it `E006_cosine_50`; the directory (and this row) is `E006_cosine50`. Best epoch 46 of 50 is itself the useful finding — the schedule was still improving at the end — corroborated by E020's best epoch 48 of 50; that is the argument for adding cosine decay to the shared code. |
| E008 | E001 | 2.5D input | Adjacent axial slices improve z-context, especially for trachea and esophagus | `in_slices=3`; otherwise identical to E001 | complete | 3D Dice: esophagus 0.508, heart 0.705, trachea 0.245; foreground mean 0.486 | Commit `b859009`; lower than E001 by 0.112 foreground mean Dice; aorta unavailable in `segthor_part1`. |
| E009_full512 | – | 512x512 inputs instead of 256x256 | Retaining resolution improves small-organ segmentation (verbatim from its record). | `--shape 512 512` on `data/SEGTHOR_512`, ENet 8/2, CE, seed 123, 25 epochs, same 15/5 split (val = 01, 11, 15, 17, 19) | complete | 3D Dice: esophagus 0.533, heart 0.697, trachea 0.444, aorta n/a; foreground mean 0.558 (HD95 27.1 / 45.4 / 31.8 mm). | A100 job `26519589`; 16.0 min; 3018 MB peak GPU; best epoch 24 of 25; best 2D val 0.8138. **VERDICT: hypothesis not supported** — foreground mean 0.558 is BELOW E001's 0.598 at 256x256, but the comparison is confounded and isolates nothing: per-patient min-max normalization (no HU window), the folded 3-organ target, and the same 8-kernel ENet at 4x the pixels. It still matters: resolution is a live candidate for the thin esophagus, and the SegTHOR CTs are already 512x512, so a 512 dataset is an identity resize in `slice_segthor.py` (one command, 4x compute, no code change) — a clean re-test with the HU window and `ce_dice` is cheap. Also in its record: iou 0.386/0.579/0.318, NSD 0.362/0.236/0.332. |
| E010_large_enet | E001 | Larger ENet capacity | A larger ENet improves segmentation under the E001 protocol, or exposes a compute/overfitting trade-off | ENet `kernels=16`, `factor=4`, `in_slices=1`; otherwise identical to E001 | complete | 3D Dice: esophagus 0.546, heart 0.828, trachea 0.480; foreground mean 0.618 | Commit `63438af`; +0.020 foreground mean Dice vs. E001; all mean HD95 values improved; aorta unavailable. |
| E010_unet2d | E001 | Plain 2D U-Net architecture/capacity | U-Net skip connections improve segmentation under the E001 protocol, or expose a compute/overfitting trade-off | U-Net base channels 32, 4 levels, `in_slices=1`; otherwise identical to E001 | complete | 3D Dice: esophagus 0.653, heart 0.768, trachea 0.636; foreground mean 0.686 | Commit `63438af`; +0.088 foreground mean Dice vs. E001, chiefly trachea; aorta unavailable. |
| E014_hu_wide | E001 | Fixed HU window [-1000, 1000] at slicing | Same contrast for every patient; esophagus no longer crushed by per-patient min-max | `dataset=SEGTHOR_huwide` (resliced, same split); otherwise identical to E001 | complete | 3D Dice: esophagus 0.673, heart 0.885, trachea 0.653; foreground mean 0.737 | Commit `d9f646d`; +0.139 fg mean Dice vs. E001 (trachea +0.276, heart +0.077, esophagus +0.063); run locally on MPS — E001 ref was Snellius; aorta unavailable. |
| E014_hu_soft | E001 | Fixed soft-tissue HU window [-200, 300] at slicing | Maximal soft-tissue contrast helps esophagus most; heart/trachea may dip from saturation | `dataset=SEGTHOR_husoft` (resliced, same split); otherwise identical to E001 | complete | 3D Dice: esophagus 0.684, heart 0.872, trachea 0.614; foreground mean 0.724 | Commit `d9f646d`; best esophagus of the three (+0.074 vs. E001), trachea dips vs. wide window as predicted (air saturates); wide window better fg mean (0.737 > 0.724); run locally on MPS; aorta unavailable. **Dominated (kept for the record)**: E014_hu_wide wins on fg mean (0.737 vs 0.724) and trachea (0.653 vs 0.614); this run's only edge is esophagus (+0.011), inside seed noise on 5 patients. Superseded on the aorta line by E017/E019. |
| E015_augment | E001 | Augmentation: rotation ±15°, scale 0.9–1.1, intensity shift ±0.1, light elastic; no h-flip | Train 0.95 / val 0.82 gap by epoch 16 is overfitting; aug narrows the gap and raises val Dice | `augment=true` with plan-1.5 params; data, split, seed identical to E001 | complete | 3D Dice: esophagus 0.640, heart 0.883, trachea 0.081; foreground mean 0.534 | Rerun with `num_workers=0` after a DataLoader shared-memory crash at epoch 4; fixed elastic mis-scaling (alpha was in grid units, ~600 px warp; now true pixel units) — heart +0.075 vs. E001 and the train/val gap narrowed, but trachea collapsed (-0.296) and fg mean below E001 (0.534 < 0.598); aorta unavailable. **Dead end (kept for the record)**: the elastic warp destroys thin structures, which is why E016/E020 test augmentation without it, and E019 shows the loss route beats augmentation on the thin classes. Artifacts trimmed to weights, curves, `best_epoch/` and `metrics3d/` in the 2026-09-17 cleanup. |
| E016_aug_noelastic | E001 | Augmentation without elastic: rotation ±15°, scale 0.9–1.1, intensity shift ±0.1, `elastic_alpha=0`; 50 epochs | E015's elastic term collapsed the thin trachea (0.081 vs 0.377); dropping it should recover trachea while keeping heart (0.883) / esophagus (0.640) gains; 50 epochs lets aug converge | `augment=true`, `aug_elastic_alpha=0`, `epochs=50`; data, split, seed identical to E001 | registered | – | Follow-up to E015. Check trachea recovery, fg mean vs E001 (0.598) and E014_hu_wide (0.737); present-only val Dice must be > 0 for all organs. |
| E017_aorta_huwide | E014_hu_wide | 4-organ target: the archive's class 1 is esophagus ∪ aorta, split back into esophagus + aorta (`retrieve_aorta.py`) | The aorta is large, high-contrast and contiguous, so it should segment well once it has a class of its own; heart/trachea labels are voxel-identical to E014, so their Dice must reproduce | `dataset=SEGTHOR_aorta_huwide`; otherwise identical to E014_hu_wide (ENet 8/2, CE, seed 123, 25 epochs, same 15/5 split). K stays 5 | complete | 3D Dice: esophagus 0.229, heart 0.900, trachea 0.664, **aorta 0.667**; foreground mean 0.615 | Commit `fd05d7b`; 52.3 min on MPS; best epoch 22; aorta is now a real class (0.667) where every earlier run had `n/a`/0.000, and heart/trachea are unchanged (0.900/0.664 vs 0.885/0.653 — identical labels, seed noise). Esophagus is NOT comparable with the folded runs: its target is the thin organ alone (0.229 here vs 0.149 for E014_hu_wide's volumes scored on the SAME 4-organ GT, so E017 is +0.080 even there). Failure mode is systematic under-segmentation of the thin esophagus (predicted/GT volume 0.15-0.60); Patient_15 is the worst case on both organs (esophagus 0.000 with zero overlap, aorta 0.537 as it captures 43 % of the widest aorta of the 20). Labels are derived, not annotated — see `AORTA_INSPECTION/aorta-findings.md`. |
| E018_aorta_augment | E017_aorta_huwide | Plan-1.5 augmentation (rotation ±15°, scale 0.9–1.1, intensity ±0.1, no h-flip) with **elastic disabled**; 25 epochs | Augmentation is the standard remedy for the organ E017 under-segments (esophagus 0.229, predicted/GT volume 0.15–0.60) and for a 15-patient training set; it should raise esophagus overlap without costing the aorta | `augment=true`, `aug_elastic_alpha=0`; `dataset=SEGTHOR_aorta_huwide`; everything else identical to E017 | complete | 3D Dice: **esophagus 0.134**, heart 0.905, trachea 0.661, **aorta 0.724**; foreground mean 0.606 | Commit `d85af56`; 44.1 min on MPS; best epoch 23. **Hypothesis rejected on the organ it targeted**: augmentation buys the aorta (+0.057) and holds heart/trachea, but the esophagus falls 0.229 → 0.134 (2D present-only 0.266 → 0.158, predicted/GT volume 0.15–0.60 → 0.12–0.28) — warping a 5–10 voxel tube costs exactly the structure it was meant to recover. Same-GT sibling E019 (`ce_dice`, no aug) reaches 0.428 on the esophagus, so the loss route is the answer for the thin class. Also probably **undertrained**: training 2D Dice 0.794 at the last epoch vs E017's 0.885, train/val gap collapsed 0.122 → 0.030, hence E020. Elastic disabled deliberately (E015's `elastic_alpha=10` collapsed the thin trachea 0.377 → 0.081); `num_workers=0` (E015's DataLoader crash). **Undertrained, not rejected**: E020 (same config, 50 epochs) takes this run's esophagus 0.134 → 0.424 and trachea 0.661 → 0.746, so the 25-epoch rejection above was an epoch-budget artefact, as the train/val gap 0.030 vs E017's 0.122 already hinted. Kept as the evidence for why the schedule matters more than the augmentation. Artifacts trimmed to weights, curves, `best_epoch/` and `metrics3d/` in the 2026-09-17 cleanup. |
| E019_aorta_ce_dice | E017_aorta_huwide | Loss: cross-entropy + soft Dice over all 5 classes (`ce_dice`) instead of plain cross-entropy | E017's failure is under-segmentation of thin/small structures; plain CE is dominated by background and penalises a missed thin organ only per voxel, while a soft Dice term optimises region overlap directly | `loss_fn="ce_dice"`; `dataset=SEGTHOR_aorta_huwide`; everything else identical to E017 | complete | 3D Dice: **esophagus 0.428**, heart 0.886, **trachea 0.787**, aorta 0.672; foreground mean **0.693** | Commit `fd05d7b`; 52 min on MPS; best epoch 16; hypothesis confirmed and stronger than expected — the Dice term roughly doubles the esophagus (+0.199 vs E017) and lifts the trachea (+0.123), the two classes cross-entropy under-segmented, while heart (−0.014) and aorta (+0.005) hold. Boundary metrics improve too (heart HD95 36.8 → 18.8 mm, trachea 57.8 → 33.2 mm). The systematic under-segmentation is gone: predicted/GT volume ratio 0.15–0.60 in E017 becomes 0.44–0.92 on three patients and over-segmentation on the two worst. **Best aorta run on fg mean and on both thin classes by the aggregate**, and the only run whose thin-organ gains survive a paired test against E017 (esophagus +0.199, trachea +0.123, both real); against E020 it shows no real loss, so treat E019 and E020 as equivalent on the thin classes and E020 ahead on heart/aorta. Patient_15's esophagus stays broken but changes failure mode (0.000 with a shrunken target → 1.86× the target with Dice 0.021: wrong-place, not missed). Single changed variable vs E017: the loss. |
| E020_aorta_augment_50ep | E018_aorta_augment | E018's augmentation extended from 25 to 50 epochs, nothing else changed | E018 rejected augmentation on the esophagus (0.134 vs E017's 0.229) but looked undertrained: training 2D Dice 0.794 at the last epoch against E017's 0.885, train/val gap 0.030 against 0.122. With enough epochs augmentation should match or beat E017 on the esophagus while keeping E018's aorta gain | `epochs=50`, `augment=true`, `aug_elastic_alpha=0`; `dataset=SEGTHOR_aorta_huwide`; everything else identical to E018 | complete | 3D Dice: **esophagus 0.424**, heart 0.907, trachea 0.746, **aorta 0.746**; foreground mean **0.706** | Commit `d85af56`; 95.3 min on MPS; best epoch **48 of 50**. **E018's rejection was an epoch-budget artefact, confirmed**: doubling the budget takes the esophagus 0.134 → 0.424 (+0.290) and trachea 0.661 → 0.746 (+0.085) with the aorta holding (0.724 → 0.746). The 2D train/val gap also closed (E018's training Dice 0.794 → converged). **Best foreground mean of every run in the project**, best heart (0.907) and best aorta (0.746). Against its `ce_dice` sibling E019, tested PAIRED per patient (same 5 patients, so patient difficulty cancels; Student t on the per-patient deltas, absolute t above 2.8 taken as real): **aorta +0.074 (t=+3.2, real)** and **heart +0.022 (t=+2.8, real)** for E020, **esophagus −0.004 (t=−0.1)** and **trachea −0.040 (t=−1.3) — both within the 5-patient noise**, so the earlier "E019 wins trachea" reading was an artefact of comparing two noisy means. E020 also has the far better heart HD95 (9.1 vs 18.8 mm). Net: E020 ≥ E019 with no real loss on any class and two real gains, and the two levers are complementary — the untested combination (this run's parent config plus `ce_dice`) is exactly what E022 tests. Best epoch 48/50 means the schedule is still improving (E006_cosine_50 peaked at 46/50 too), which is the argument for cosine decay. Patient_15's esophagus (0.014) is untouched by any of the four arms and is the single largest contributor to the noise floor: its per-patient esophagus spread is SD 0.235, giving a 5-patient standard error of 0.105 unpaired — with this validation set only the paired comparisons above are decisive. |
| E021_aorta_lcc_postprocess | – | Plan 1.7, largest 3D connected component per class at inference (no training) | Each organ is a single connected structure, so stray false-positive blobs cost Dice and wreck HD95; filtering them should drop HD95 sharply at zero training cost | Applied post hoc to the stitched volumes of E017/E018/E019, scored through the same `metrics3d.class_metrics` path | **rejected** | Dice Δ: esophagus −0.065 / −0.051 / −0.016, heart +0.005 / +0.001 / +0.001, trachea −0.031 / −0.040 / +0.003, **aorta −0.046 / −0.001 / −0.077**; HD95 does drop a lot for heart (36.8→10.0 mm E017) and trachea (57.8→24.9 mm), but rises for esophagus (58→136 mm) and aorta (23→59 mm) | Hypothesis **refuted for the classes we care about**, and the reason is now measured: the predictions fail by leaving *gaps*, which fragment the blob, so "keep the largest piece" deletes correctly-segmented parts beyond the gap. On E019 the aorta prediction is 6–19 pieces with the largest holding 49 % (Patient_15), 64 % (Patient_11), 96–100 % (the easy patients); the esophagus is 3–11 pieces with 59 % on Patient_15. Where the prediction is already one blob (Patients 01/17/19) the filter is harmless but adds nothing. GT connectivity confirms the premise only half holds: esophagus, heart and trachea are single-component in all 5 validation patients, but the recovered aorta has a second component on Patients 01 and 11 (1 and 1813 voxels). Dropped: not shipped, and no training time spent. A targeted variant — largest component *per slice* for the thin classes, which cannot cut across a gap — is untested. |
| E022_aorta_ce_dice_augment_75ep | E020_aorta_augment_50ep | Combine both levers: `ce_dice` loss + plan-1.5 augmentation (elastic disabled) + 75 epochs | E019 and E020 reach the same esophagus by different routes and each has real gains the other lacks — paired per-patient over the same 5 patients, E019 vs E017 gives esophagus +0.199 (t=+3.3) and trachea +0.123 (t=+5.0), E020 adds aorta +0.079 (t=+3.7) and heart +0.022, and E020 vs E019 shows no real loss on any class (esophagus −0.004, trachea −0.040, both inside noise) — so the two are complementary and combining them should beat both; the 75-epoch extension is justified because E020's best epoch was 48 of 50 with the curve still rising. | `loss_fn="ce_dice"`, `augment=true`, `aug_elastic_alpha=0`, `epochs=75`, `num_workers=0`; `dataset=SEGTHOR_aorta_huwide`; otherwise identical to E020 | complete | 3D Dice: **esophagus 0.491**, heart **0.921**, trachea **0.820**, aorta **0.796**; foreground mean **0.757** | Commit `b54052c`; 133.0 min on MPS; best epoch **71 of 75**. **Best run in the project on every class at once** — fg mean 0.757 vs E020's 0.706 (+0.051) and E019's 0.693, so the two levers are additive as predicted. Paired per patient, only heart (+0.036 vs E019, t=+4.9) and aorta (+0.124 vs E019, t=+4.8; +0.050 vs E020, t=+2.2) clear the noise bar; the esophagus gains are suggestive, not decisive (+0.068 vs E020 t=+2.6, +0.063 vs E019 t=+2.6) — but the direction is unanimous: **all 5 patients improve on the esophagus against both parents** (P01 .500→.662, P11 .460→.485, P15 .014→.030, P17 .598→.652, P19 .547→.628), a 5/5 sign test at p=0.0625. Trachea 0.820 beats E019's 0.787; heart HD95 18.8 → 11.2 mm. Patient_15's esophagus moved for the first time in the project (0.000 in E017/E018 → 0.030) but remains the standing failure. Best epoch 71 of 75 — the third run in a row still rising at the cutoff (E020 48/50, E006_cosine_50 46/50), so the epoch budget is still binding and a decaying LR (plan 1.6) is now the best-supported next lever. |
| E025_aorta_norm | E001_baseline | E001's protocol on the recovered four-organ target at the baseline normalisation: the aorta baseline that every earlier row lacked | Every row up to E023 reports "aorta unavailable" and every foreground mean is a 3-class mean, because the folded archive has no class 4. Repeating E001 on `SEGTHOR_aorta_norm` gives the missing baseline column and makes all four organs comparable | `dataset=SEGTHOR_aorta_norm` (per-patient min-max, E001's normalisation, aorta split out by `retrieve_aorta.py`); otherwise identical to E001 | complete | 3D Dice: esophagus 0.233, heart 0.789, trachea 0.349, **aorta 0.578**; foreground mean **0.487** (4-class) | 59.0 min on MPS; best epoch 23 of 25. Dataset verified before the run: identical filenames in both splits (2817/915), identical 15/5 fold, byte-identical `img/` PNGs; the only change is that GT class 1 is split into 1 and 4, so heart and trachea stay comparable to the folded rows while esophagus is not. **The fg mean is NOT comparable to E001's 0.598** — that is a 3-class mean on the folded target where "esophagus" was the easier esophagus-union-aorta. Scoring note: `stitch.py` needs `--num_classes 5`; passing 255 leaves the ×63 PNG values undecoded and every metric reads 0.000. |
| E026_aorta_augment_50ep | E025_aorta_norm | Plan-1.5 augmentation (rotation ±15°, scale 0.9–1.1, intensity ±0.1, **elastic disabled**) on the baseline normalisation, 50 epochs | Augmentation is the standard remedy for a 15-patient training set. Every existing augmentation result with an aorta runs on `SEGTHOR_aorta_huwide`, so the augmentation effect was confounded with HU clipping; this run separates them | `augment=true`, `aug_elastic_alpha=0`, `epochs=50`; `dataset=SEGTHOR_aorta_norm`; everything else identical to E024 | complete | 3D Dice: **esophagus 0.447**, heart 0.890, **trachea 0.636**, **aorta 0.689**; foreground mean **0.665** | 92.1 min on MPS; best epoch **46 of 50**. Hypothesis confirmed, and the gain is about twice the HU-clipped arm's: fg mean **+0.178 vs E024** (against E020's +0.091 vs E017) and aorta **+0.111** (against +0.079), because E024 had more headroom. Every organ improves; the thin organs gain most (trachea +0.287, esophagus +0.214) and the aorta least of the foreground classes. Elastic stays off because E015's alpha 10 collapsed the trachea (0.377→0.081). Deviates from E024 in epochs as well as augmentation, since the best epoch at 46 of 50 shows 25 would undertrain it — the same artefact E018's rejection hit. |
| E027_aorta_weighted_ce_dice_50ep | E025_aorta_norm | Weighted CE + soft Dice, with `class_weights` derived from the measured class imbalance of the four-organ target | The aorta line has only ever tested region-overlap loss (`ce_dice`) against imbalance. Class weighting is implemented and six configs exist under `configs/losses/`, but no weighted run was ever registered, so it is untested rather than refuted. Weighting should recover the thin organs the Dice term alone under-serves | `loss_fn="weighted_ce_dice"`, `class_weights=[0.450, 23.181, 5.051, 24.162, 10.066]`, `epochs=50`, no augmentation; `dataset=SEGTHOR_aorta_norm`; everything else identical to E024 | complete | 3D Dice: **esophagus 0.377**, heart 0.848, **trachea 0.581**, aorta 0.599; foreground mean **0.601** | 110.7 min on MPS; best epoch **25 of 50**, flat for the remaining 25. Weights are the **square root** of inverse class frequency over the train fold (background 98.947%, esophagus 0.0372%, heart 0.784%, trachea 0.0343%, aorta 0.197%), normalised to background = 0.450; the sqrt compresses the trachea's raw 583.8 to 24.2 because the loss is `weighted_ce + dice_loss` and a spiky vector collapses onto the smallest class. Result: **+0.114 fg mean vs E024**, with the gain concentrated in the thin organs (trachea +0.232, esophagus +0.144) and the aorta barely moving (**+0.021**) — expected, since the aorta was never starved of gradient. **Do NOT compare against E019 directly**: the two differ in loss, dataset and epochs at once, so the apparent −0.092 fg mean is HU clipping being removed, not the weighting hurting. The single-variable weighted-vs-unweighted comparison does not exist yet. Patient_19 trachea 0.101 (class std 0.263) is the failure to inspect. |
